<!DOCTYPE html>
<html lang='fr'>
  <head>
    <meta charset='UTF-8'>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mes expériences</title>
    <link rel='stylesheet' href='/public/css/shared.css'>
    <link rel='stylesheet' href='/public/css/shared/header.css'>
    <link rel='stylesheet' href='/public/css/shared/contact.css'>
    <link rel='stylesheet' href='/public/css/shared/tag.css'>
    <link rel='stylesheet' href='/public/css/experiences/experience.css'>
    <link rel='stylesheet' href='/public/css/experiences/summary-card.css'>
  </head>

  <body>
    <%= render('shared/header.erb') %>

    <main class='main-content-after-header'>
      <h1 class='container'>Mes expériences de développeur Ruby on Rails</h1>
      <div class='container experience-summary-cards'>
        <% work_experiences.each do |work_experience| %>
          <%= render(
            'experiences/summary_card.erb',
            company_name: work_experience[:company_name],
            start_date: work_experience[:start_date],
            end_date: work_experience[:end_date],
            tags: work_experience[:tags]
          ) %>
        <% end %>
      </div>
    </main>

    <%= render('shared/contact.erb') %>
  </body>
  <script src="https://kit.fontawesome.com/7ab03c2740.js" crossorigin='anonymous'></script>
</html>